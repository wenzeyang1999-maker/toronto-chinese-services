# 开发工作日志 — Toronto Chinese Services

---

## 2026-03-28

### 即时通讯系统（实时聊天）
- **聊天页面重构**：修复发送消息后内容不显示的问题，引入「乐观更新」机制——用户点击发送后消息即刻出现在界面，无需等待服务器响应；若发送失败自动回滚并恢复输入框内容
- **实时去重**：Supabase Realtime 推送的消息与乐观消息通过 ID 比对去重，防止同一条消息显示两次
- **桌面端布局修复**：聊天界面在 PC 端改为居中卡片（max-w-2xl，90vh 高度），不再撑满整个屏幕

### 未读消息提醒系统
- **全局消息按钮**：右下角新增悬浮「消息」按钮（白色胶囊样式），登录后可见；有未读消息时按钮右上角显示红点，点击直接跳转消息中心
- **消息列表未读角标**：消息中心对话列表每条会话显示未读数量角标，打开对话后自动清零
- **实时刷新**：以上未读状态均通过 Supabase Realtime 实时更新，无需手动刷新页面
- **URL 跳转支持**：`/profile?section=messages` 直接打开消息中心页，消息按钮点击后精准跳转

### 服务详情页（ServiceDetail）
- **响应式宽度**：桌面端内容区从 max-w-2xl 扩展至 max-w-4xl，不再过窄
- **图片展示升级**：
  - 移动端：仿小红书横向滑动轮播，底部圆点指示当前位置
  - 桌面端：动态宫格布局（1 张全宽、2 张并排、3-4 张四宫格、5+ 张三列）
- **发消息错误处理**：点击「发消息」失败时弹出提示，不再静默无响应

### AI 助手按钮位置调整
- 移动端 AI 对话按钮上移（bottom-24），避免与服务详情页底部操作栏重叠

### 发布服务页面（PostService）
- **分类精简为 6 个**：5 个主分类（搬家、保洁、接送、装修、现金工）+ 「其他服务」，去除多余按钮
- **服务搜索大幅扩展**：内置服务库从约 20 条扩展至约 80 条，新增：
  - 体育教练：游泳、网球、羽毛球、篮球、乒乓球、高尔夫、滑冰、滑雪、健身、武术、跆拳道、驾照培训
  - 文艺教学：钢琴、吉他、小提琴、声乐、舞蹈、瑜伽
  - 学科家教：中文、数学、英文、法语
  - 专业服务：报税、法律、移民、保险、留学、房产、贷款、翻译
  - 生活服务：美甲、美发、按摩、针灸、宠物、除雪、婚庆、摄影、视频剪辑、平面设计、老人护理、儿童托管等
- **众包服务类型数据库**：新增 `service_types` 数据表，用户发布新服务类型时自动保存，下次其他用户搜索时可直接找到
- **搜索结果实时合并**：搜索框同时检索内置库和数据库，结果以可点击按钮展示

### Bug 修复（代码审查发现）
- **加载页卡死修复**：`fetchServices()` 网络失败时不再永久停留在加载动画，改为失败也继续进入主页
- **消息列表加载卡死修复**：消息中心、聊天页面数据请求失败时正确退出加载状态，不再显示无尽转圈
- **发布服务「继续发布」修复**：重置表单时同步清空已选地区、自定义服务名称、图片预览及 Blob URL，避免上次内容残留
- **Blob URL 内存泄漏修复**：发布页卸载时自动释放所有图片预览的 Blob URL

---

## 2026-03-26

### 服务数据接入 Supabase 数据库
- **发布服务写入数据库**：PostService 表单提交后，数据正式保存到 Supabase `services` 表，不再只存在于内存
- **首页/搜索读取真实数据**：App 启动时调用 `fetchServices()`，首页和搜索页展示的是数据库里的真实服务
- **登录校验**：未登录用户点击发布服务，自动跳转到登录页
- **用户资料同步**：发布服务时自动更新用户的姓名、电话、微信号到 `users` 表
- **图片上传框架**：代码层面已支持上传最多 3 张图片到 Supabase Storage（需配置 `service-images` bucket，待后续完善）

### 数据库权限修复
- 修复 `users` 表 RLS 策略：改为公开可读，解决首页加载服务商信息报错问题
- 补充 `services` 表 INSERT 权限，允许登录用户发布服务
- `services` 表新增 `images TEXT[]` 列存储图片链接
- 补充 `other`（其他服务）分类到数据库

### Schema 完善（一次部署即可用）
- 所有建表、策略、触发器语句加上防重复 guard，新建 Supabase 项目只需跑一次 `schema.sql`

---

## 历史工作（2026-03-25 及之前）

### 网站上线 & 部署流程
- 代码托管至 GitHub，连接 Vercel 自动部署（每次 push main 分支自动更新）
- Supabase 环境变量配置到 Vercel（`VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`）

### 用户注册 / 登录 / 个人中心
- 注册页：邮箱+密码+姓名+电话，重复邮箱提示跳转登录
- 登录页：邮件未验证单独提示
- 个人中心页：展示姓名、邮箱、电话，支持退出登录
- 导航栏：登录后显示「我的」图标，未登录显示「注册」「登录」按钮

### 发布服务页面
- 服务类型：图标网格 + 搜索框 + 自定义类型（输入后生成 tag）
- 服务图片：最多 3 张，本地预览，可删除
- 服务地区：多选 tag，支持搜索 50+ 安省城市（中英文）
- 表单填写后自动滚动到下一个区块，提升移动端体验

### 首页 & 响应式布局
- 导航栏：移动端汉堡菜单，桌面端全展开
- 内容宽度：手机 97%，平板 85%，桌面 70%
- 首页展示附近/最新服务卡片，分类快捷入口

### 数据库 Schema
- 建立 `users`、`categories`、`services`、`service_images`、`reviews` 表
- Supabase Auth 注册时自动触发插入 `public.users` 行
- RLS 权限策略全部配置完毕
