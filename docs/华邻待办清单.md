# 华邻（HuaLin）待办清单 — 对照说明书 V5.1 / V5.2 尚未完成的部分

> 依据：《重构版 V5.1》《网页架构补充 V5.2》两份说明书，逐条比对现有代码库后，仅列出**尚未完成或需完善**的项。已经做好的功能（导航、瀑布流、搜索、已读置灰、收藏、AI 发单提取、5 人抢单、社媒外链等）不在此清单内。
>
> 工期为粗估，供排期参考。更新于比对当天。

---

## 🔴 A. 确实还没做（说明书明确要求，需新建）

- [ ] **A1. 完工拍照存证** — 师傅点"完工"时强制上传 1–3 张现场照片，写入存证表作为纠纷证据。
  - 涉及：完工按钮流程、图片上传、新增存证表。｜粗估 2–3 天
- [ ] **A2. AI 自动仲裁** — 发生纠纷时，由 AI 调取聊天记录 + 发单要素 + 完工照做初步判定。
  - 依赖 A1 的存证数据。｜粗估 2–3 天
- [x] **A3. 名片墙"持牌"绿色芯片 + "商业保险"盾牌芯片** — ✅ 已完成（2026-07-13）。迁移 `20260713120004` 加 `has_license`/`has_insurance` 字段（锁定防自我认证，SECURITY DEFINER + owner-update WITH CHECK）+ admin 专用 `admin_set_provider_flags` RPC；后台 VerificationTab 加「持牌/保险」切换；名片墙显示绿色「持牌」+ 蓝色「保险」芯片。⚠️ 需在 Supabase 跑迁移。AI-OCR 自动识别仍是 C3（未做）。
- [x] **A4. 名片墙"距离远近"排序** — ✅ 已完成（2026-07-13）。InquiryResultPanel 用客户 inquiry 坐标 × 服务商 online 坐标算距离（`calcDistance`），名片显示「X.X km」并就近排序（无坐标者沉底）。注：服务商需在线才有坐标；未来若要离线也可算，需补服务商注册地/邮编。
- [x] **A5. 聊天界面"身份烙印"配色** — ✅ 已完成（2026-07-13）。ConversationPage 顶部（header 下方）加身份色条：客户视角暖色（琥珀/橙渐变）「🛒 您是客户 · 正在向「X」咨询「服务」」，服务商视角深色（slate/蓝黑渐变）「🔧 您是服务商 · 正在为「X」提供「服务」」。纯前端。
- [ ] **A6. 成交订单闭环 + 名片"接单量"** — 说明书 §4.3 要求名片墙展示「接单量（累计履约完成的**真实总订单数**）」，§6.2 定义 `orders` 表含 `completed` 状态。现有「5 人抢单」走 `inquiries`，但**没有"成交/完工 → 计入接单量"的闭环**，名片上也没有接单量指标。需建成交状态机（双向确认 / 完工 → completed）驱动 `order_count`，并把 A1 完工存证接到"完工"事件上。
  - 设计方案已写：`docs/ORDERS_DESIGN.md`。｜粗估 L（1 周+，建表 + 状态 RPC + 我的订单页 + 名片"已成交 N 单"战绩条）
- [x] **A7. 名片"经营主体"标记（自雇 Individual / 公司 Company）** — ✅ 已完成（2026-07-13）。字段 `business_type`（individual/business）本已存在且可在资料页设置；本次在名片墙 InquiryResultPanel 补上「👤 自雇 / 🏢 公司」标记。无需迁移。

## 🟡 B. 做了一半，需要补齐到说明书标准

- [ ] **B1. 城市定位自动跳转** — 现在只有"是否在多伦多服务区"的提示横幅；需补：开屏自动判定所在城市 + 已开通城市自动切换。｜粗估 1–2 天
- [ ] **B2. 未开通城市拦截弹窗** — 补说明书要求的半透明轻量弹窗（"当前城市暂未开通服务，华邻正在解锁中…"）。｜粗估 0.5 天
- [ ] **B3. 三级地区联动选择（大洲→国家→城市）** — 现在只有大多伦多区域选择，需扩成三级联动，支持跨城帮亲友发单。
  - 需准备地区节点树数据。｜粗估 2 天
- [x] **B4. 一键导航补 Apple Maps** — ✅ 已完成（2026-07-13）。抽 `src/lib/navigation.ts`（iOS→`maps://`，其余→Google Maps），ServiceDetail「一键导航」、Events 导航（mapNavUrl）、RequestDetail 均改用；RealEstate 的「在地图查看」保持查看语义不动。
- [ ] **B5. "一键身份翻转"改成醒目大按钮** — 现有客户/师傅切换藏在设置里，需按说明书做成个人中心/名片处的显眼一键翻转 UI。｜粗估 1 天
- [ ] **B6. 二级分类标签精细化** — 现有筛选标签可用；按说明书补"点一级导航后下方丝滑滑出分类芯片、异步无缝刷新"的交互质感。｜粗估 1 天
- [ ] **B7. 录用后再解锁精确地址** — 现在发单地址已做模糊偏移；需补"客户点【确认录用】后，才向中选师傅开放精确门牌地址"的明确闸口。｜粗估 1 天

## 🔵 C. 需老板拍板的技术选型（不确认不动工）

- [ ] **C1. AI 是否必须换成 OpenAI GPT-4o-mini** — 现用 Groq（功能等价、通常更快更便宜）。说明书写的是 OpenAI。若要求严格对齐则需切换。｜切换约 0.5–1 天
- [ ] **C2. 数据库是否要重构成说明书的表名/字段** — 现用 `users` + `inquiries`（功能已跑通），说明书写的是 `profiles` + `orders` 的字段集。功能等价，是否要为对齐文档而改名迁移？｜迁移约 1–2 天（有风险，建议不改）
- [ ] **C3. 持牌资质是否上 AI-OCR 自动识别** — A3 若要"上传执照后 AI 自动识别核验"，需额外接 OCR 服务。｜粗估 +2 天

## 🟢 D. 老板新指示（2026-07-13）— 地图模式精简

> 背景：老板提出"取消地图模式"。经分析，地图在平台有 4 种用途，价值差别很大，建议**按场景精简而非全砍**（详见 `docs/地图去留-利弊分析.pdf`）。以下按推荐范围拆解。
>
> 📌 补充：这**与说明书方向一致**——《重构版 V5.1》§1.3「技术架构至简」原文即要求"前端彻底砍掉高并发实时动态地图展示，地图全面后台化，仅作为接单录用后一键导航的履约工具，API 资费及并发压力骤降 90%"。说明书甚至比现状（地图仍可选）更激进。故地图精简不是临时改动，是回归说明书设定。

- [x] **D1. 列表优先，地图可选（不再默认地图）** — ✅ 已完成（2026-07-13）。按最终定调：**保留地图切换按钮**，只把首页「找服务」的默认视图从地图改为列表；用户手动切到地图仍会被记住（localStorage）。全站列表页现已统一为"列表默认、地图可选"（Category/Search/RealEstate 本就是列表默认）。tsc 0 错误。
  - 改动文件：`src/pages/Home/Home.tsx`（默认 `'map'` → `'list'`）
- [ ] **D2. 保留三处高价值地图（确认保留，无需开发）** — ① 房产找房地图 ② 服务详情「位置 + 一键导航」③ 商家「发现客户」看附近需求。此三处砍掉会伤核心工作流，默认不动。
- [ ] **D3.（可选 · 降本）服务详情交互地图 → 静态图 + 导航链接** — 若主要动机是压 Google Maps API 成本，把服务详情的交互式地图换成静态缩略图 + 一键导航 deeplink，费用大降、体验基本不掉。｜粗估 0.5–1 天
- [ ] **D4.（⚠️ 待老板确认范围）是否"彻底移除全部地图"** — 若老板要求连房产 / 发现客户也一并下线，这两条工作流将失去地理浏览能力，影响面大。**动工前务必确认，并建议先看地图实际使用率数据再定。**
  - 与之相关：`B4 一键导航补 Apple Maps` 若保留导航则继续有效；`A4 距离远近排序` 用经纬度计算、不依赖地图 UI，保留不受影响。

---

### 汇总
- 🔴 确实没做：**7 项**（A1–A7；A6 成交闭环为大工程 L）
- 🟡 需补齐：**7 项**（B1–B7）
- 🔵 待拍板：**3 项**（C1–C3）
- 🟢 老板新指示（地图精简）：**4 项**（D1–D4，其中 D1 已完成、D2 无需开发、D4 待确认范围）
- 粗估总工期（不含待拍板项 + 不含 A6 成交闭环大工程）：约 **3–4 周**；A6 成交闭环另计约 **1 周+**

> **A6 成交闭环**是说明书里被漏进对照的一环：它同时是「接单量」名片指标（§4.3）和 `orders` 成交状态机（§6.2）的地基，也是审计里「墙 1」。工程量大，建议单独立项排期，见 `docs/ORDERS_DESIGN.md` / `docs/SOLVE_LIST.md`。

> 已完成部分请见对照表 PDF：`docs/华邻需求实现对照表.pdf`。
> AI 检查报告误判的澄清见：`docs/AI检查报告核实说明.pdf`。
